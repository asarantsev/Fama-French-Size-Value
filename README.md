# Fama-French-Size-Value
Consider size and value returns. We have autoregression fit with and without volatility. For the value, we are successful, but for the size, we failed. verification.py is the auxillary function which studies regression residuals. 
